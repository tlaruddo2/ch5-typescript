import React from "react";
import { Box, Typography } from "@mui/material";

type Props = {};

export const Shades = (props: Props) => {
  return (
    <Box sx={{ marginLeft: { xs: 8, sm: 25 } }}>
      <Typography>Shades page</Typography>
    </Box>
  );
};
