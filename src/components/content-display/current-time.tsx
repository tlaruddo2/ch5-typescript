import { Box, Typography } from "@mui/material";

type Props = {};

export const CurrentTime = (props: Props) => {
  return (
    <Box sx={{ marginLeft: 5 }}>
      <Typography>curent time</Typography>
    </Box>
  );
};
